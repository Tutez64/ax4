/**
 * Package-level var whose name matches the Haxe wrapper class (PascalCase).
 * Wrapped as: final class SharedState { public static var SharedState_:Int; }
 * (field renamed to avoid Haxe ClassName.ClassName resolution ambiguity)
 */
package wrapModuleLevelDecls {
	public var SharedState:int = 0;
}
