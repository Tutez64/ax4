-cp src
-lib format
-D no-copt

-D analyzer-optimize

# --macro nullSafety("ax4")

-main ax4.Main

# --- MODERNIZATION FIX ---
# Instead of generating to a folder and copying (which fails on Windows),
# we compile directly to the final JAR using the new Haxe 4.3 syntax.
--jvm converter.jar

# --- OLD CONFIG (Commented out) ---
# -java java_out
# -D jvm
# -cmd cp java_out/Main.jar converter.jar

# -cmd java -jar converter.jar config-local.json
